# Atomycal Public Website

## Dev

```bash
# To start:
$ git clone git@github.com:atomycal/web.git
$ cd web
$ yarn
$ yarn dev

## To deploy
$ yarn build
$ yarn preview # check if everything is alright
$ <git push to master repo>
```